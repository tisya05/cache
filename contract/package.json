{
  "name": "@cache/contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "compact compile src/cache.compact src/managed/cache",
    "compile:fast": "compact compile -- --skip-zk src/cache.compact src/managed/cache",
    "clean": "rm -rf src/managed"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "^0.19.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
